# Core data / utilities
numpy
pandas
matplotlib
pillow            # image loading
scikit-learn      # metrics, train/test split, baselines
jupyter           # notebooks for exploration

# Web app
streamlit
streamlit-paste-button   # paste an image straight from the clipboard

# Deep learning stack.
# The line below makes a fresh install (e.g. Streamlit Cloud, which is CPU-only)
# pull the CPU build. On your machine the GPU build is already installed, so pip
# leaves it untouched. For a fresh LOCAL GPU setup, install the CUDA build per
# README step 4 (it overrides this).
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
