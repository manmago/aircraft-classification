# Core data / utilities
numpy
pandas
matplotlib
pillow            # image loading
scikit-learn      # metrics, train/test split, baselines
jupyter           # notebooks for exploration

# Deep learning stack — installed SEPARATELY, not from this file, because the
# NVIDIA GPU (CUDA) build comes from PyTorch's own package index. See README
# step 4 for the exact command.
# torch
# torchvision
